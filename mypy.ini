[mypy]
exclude = (?x)(
    # Exclude generated files in out/ directory
    out/.*
    |
    # Exclude docs directory
    docs/.*
    )

# Enforce strictness for the rust_ephem package
[mypy-rust_ephem.*]
strict = True

# Strict checking for tests too
[mypy-tests.*]
strict = True
